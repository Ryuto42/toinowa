'use client';
import { useState } from 'react';
function decode(value:string){const padding='='.repeat((4-value.length%4)%4);const raw=atob((value+padding).replace(/-/g,'+').replace(/_/g,'/'));return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
export function PushToggle(){const[status,setStatus]=useState('');async function enable(){if(!('serviceWorker'in navigator)||!('PushManager'in window)){setStatus('このブラウザは通知に対応していません');return}const config=await fetch('/api/push/config').then(r=>r.json());if(!config.publicKey){setStatus('通知は準備中です');return}const permission=await Notification.requestPermission();if(permission!=='granted'){setStatus('通知が許可されませんでした');return}const registration=await navigator.serviceWorker.register('/sw.js');const subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:decode(config.publicKey)});const response=await fetch('/api/push/subscriptions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(subscription.toJSON())});setStatus(response.ok?'ブラウザ通知を有効にしました':'通知を登録できませんでした')}return <div className="rounded-xl border border-slate-200 bg-white p-4"><p className="font-bold">ブラウザ通知</p><p className="mt-1 text-xs text-slate-500">復習予定と先生からの返信をまとめて通知します。</p><button onClick={enable} className="mt-3 rounded-lg border border-emerald-700 px-3 py-2 text-sm font-bold text-emerald-700">通知を有効にする</button>{status?<p className="mt-2 text-xs text-slate-600">{status}</p>:null}</div>}
