'use client';
import { useState } from 'react';
interface User { id:string;display_name:string;email:string;role:string;status:'active'|'invited'|'suspended' }
export function UserList({initial}:{initial:User[]}){const[users,setUsers]=useState(initial);async function change(id:string,status:User['status']){const response=await fetch(`/api/admin/users/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status})});if(response.ok)setUsers(current=>current.map(user=>user.id===id?{...user,status}:user))}return <div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="text-xs text-slate-500"><tr><th className="pb-3">氏名</th><th>ロール</th><th>メール</th><th>状態</th><th>操作</th></tr></thead><tbody className="divide-y divide-slate-100">{users.map(user=><tr key={user.id}><td className="py-3 font-bold">{user.display_name}</td><td>{user.role}</td><td>{user.email}</td><td>{user.status}</td><td><select aria-label={`${user.display_name}の状態`} value={user.status} onChange={e=>change(user.id,e.target.value as User['status'])} className="rounded-lg border border-slate-300 px-2 py-1"><option value="active">有効</option><option value="invited">招待中</option><option value="suspended">停止</option></select></td></tr>)}</tbody></table></div>}
