import { requireRole } from '@/lib/auth/guard'; import { adminDb } from '@/lib/database/admin'; import { PageTitle, Panel } from '@/components/dashboard'; import { UserList } from '@/components/admin/user-list';
export default async function UsersPage(){const context=await requireRole('admin');const{data,error}=await adminDb().from('users').select('id,display_name,email,role,status').eq('tenant_id',context.tenantId).order('role').order('display_name');if(error)throw new Error(error.message);return <div><PageTitle title="ユーザー管理" description="停止したユーザーは次回のJWT更新時にapp_role=noneとなり、アクセスできなくなります。"/><Panel title="ユーザー"><UserList initial={data??[]}/></Panel></div>}
