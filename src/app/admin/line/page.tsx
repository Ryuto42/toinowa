import { requireRole } from '@/lib/auth/guard'; import { PageTitle, Panel, StatusPill } from '@/components/dashboard'; import { isLineConfigured } from '@/lib/shared/env.server';
export default async function LinePage(){await requireRole('admin');return <div><PageTitle title="LINE連携" description="LINEはM16で実装します。秘密情報は画面に表示せず、接続状態だけを確認できます。"/><Panel title="接続状態"><div className="flex items-center justify-between"><div><p className="font-bold">Messaging API</p><p className="mt-1 text-sm text-slate-500">チャネルシークレットとアクセストークン</p></div><StatusPill tone={isLineConfigured?'emerald':'amber'}>{isLineConfigured?'設定済み':'準備中'}</StatusPill></div><div className="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-600">LINEアカウントの連携コード、署名検証、Webhook処理は次のマイルストーンで有効になります。</div></Panel></div>}
