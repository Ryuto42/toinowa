import { z } from 'zod'; import { requireRole } from '@/lib/auth/guard'; import { createClient } from '@/lib/database/server'; import type { Json } from '@/lib/database/types'; import { json, parseJson, routeError } from '@/lib/api/http';
const schema=z.object({dailyTimeLimitMin:z.number().int().min(5).max(240).nullable().optional(),learningPreferences:z.array(z.string().trim().min(1).max(100)).max(20).optional(),notificationSettings:z.record(z.string(),z.unknown()).optional(),consentStatus:z.record(z.string(),z.unknown()).optional()}).refine(v=>Object.keys(v).length>0,'変更内容がありません');
export async function GET(){try{const context=await requireRole('student');const{data,error}=await(await createClient()).from('student_profiles').select('*').eq('user_id',context.userId).single();if(error)throw new Error(error.message);return json({settings:data})}catch(error){return routeError(error)}}
export async function PATCH(request:Request){try{const context=await requireRole('student');const body=await parseJson(request,schema);const{data,error}=await(await createClient()).from('student_profiles').update({...(body.dailyTimeLimitMin!==undefined?{daily_time_limit_min:body.dailyTimeLimitMin}:{}),...(body.learningPreferences?{learning_preferences:body.learningPreferences}:{}),...(body.notificationSettings?{notification_settings:body.notificationSettings as unknown as Json}:{}),...(body.consentStatus?{consent_status:body.consentStatus as unknown as Json}:{})}).eq('user_id',context.userId).eq('tenant_id',context.tenantId).select('*').single();if(error)throw new Error(error.message);return json({settings:data})}catch(error){return routeError(error)}}
